import { TransferScreen } from "@/components/(Transactions)/Transfer";
import React from "react";
import { StyleSheet } from "react-native";

const transfer = () => {
  return <TransferScreen />;
};

export default transfer;

const styles = StyleSheet.create({});
